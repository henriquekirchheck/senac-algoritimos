algoritmo "??"
var
    soma: real
    numero: real
    i: inteiro
inicio
    para i de 1 ate 3 faca
        escreva("Digita numero: ")
        leia(numero)
        soma <- soma + numero
    fimpara
    escreval("Soma:", soma)
fimalgoritmo