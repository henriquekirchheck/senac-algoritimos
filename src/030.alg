algoritmo "??"
var
    preco: real
inicio
    escreva("Digite preço: ")
    leia(preco)

    se (preco > 100) entao
        escreval ("O preço é:", preco * 0.9)
    senao
        escreval ("O preço é:", preco)
    fimse
fimalgoritmo