algoritmo "??"
var
    i: inteiro
    soma: inteiro
inicio
    i <- 1
    soma <- 0
    enquanto (i < 100) faca
        soma <- soma + i + 1
        i <- i + 2
    fimenquanto
    escreval("Soma de pares de 1 a 100: ", soma)

fimalgoritmo