algoritmo "??"
var
    i: inteiro
    soma: real
    numero: real
inicio

    repita
        escreva("Digita numero: ")
        leia(numero)
        soma <- soma + numero
        i <- i + 1
    ate i = 4

    escreval("Media:", soma / i)

fimalgoritmo