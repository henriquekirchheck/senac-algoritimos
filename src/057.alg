algoritmo "??"
var
    counter: inteiro
    input: inteiro
inicio
    escreva("Digita numero: ")
    leia(input)
    counter <- 0
    enquanto (counter < input) faca
        escreva(counter + 1)
        counter <- counter + 1
    fimenquanto
fimalgoritmo