algoritmo "??"
var
    i: inteiro
    numero: inteiro
inicio
    escreva("Digita numero: ")
    leia(numero)
    repita
        escreval(numero, " *", i + 1, " =", numero * (i + 1))
        i <- i + 1
    ate i = 10

fimalgoritmo