algoritmo "??"
var
    counter: inteiro
inicio
    counter <- 0
    enquanto (counter < 100) faca
        se ((counter + 1) % 3 <> 0) entao
            escreva(counter + 1)
        fimse
        counter <- counter + 1
    fimenquanto
fimalgoritmo